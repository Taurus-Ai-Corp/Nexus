version: '3.8'

services:
  # ===========================================
  # BIZFLOW BACKEND - FastAPI
  # ===========================================
  bizflow-backend:
    build:
      context: ./01-CORE-PLATFORM/bizflow-backend
      dockerfile: Dockerfile
    container_name: bizflow-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${SUPABASE_DATABASE_URL}
      - REDIS_URL=redis://redis:6379
      - HUBSPOT_ACCESS_TOKEN=${HUBSPOT_ACCESS_TOKEN}
      - MONGODB_URI=${MONGODB_URI}
      - PLANETSCALE_URL=${PLANETSCALE_URL}
    depends_on:
      - redis
    volumes:
      - ./01-CORE-PLATFORM/bizflow-backend:/app
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # BIZFLOW FRONTEND - Next.js
  # ===========================================
  bizflow-frontend:
    build:
      context: ./01-CORE-PLATFORM/bizflow-frontend
      dockerfile: Dockerfile
    container_name: bizflow-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://bizflow-backend:8000
    depends_on:
      - bizflow-backend
    volumes:
      - ./01-CORE-PLATFORM/bizflow-frontend:/app
      - /app/node_modules
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # NEOVIBE STUDIO
  # ===========================================
  neovibe-studio:
    build:
      context: ./01-CORE-PLATFORM/neovibe-studio
      dockerfile: Dockerfile
    container_name: neovibe-studio
    ports:
      - "3001:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://bizflow-backend:8000
    depends_on:
      - bizflow-backend
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # REDIS - Caching & Sessions
  # ===========================================
  redis:
    image: redis:7-alpine
    container_name: bizflow-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - bizflow-network
    restart: unless-stopped
    command: redis-server --appendonly yes

  # ===========================================
  # POSTGRES - Local Development (mirrors Supabase)
  # ===========================================
  postgres:
    image: postgres:15-alpine
    container_name: bizflow-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-bizflow}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-bizflow_dev}
      - POSTGRES_DB=${POSTGRES_DB:-bizflow_dev}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./05-DATABASES/supabase/schemas:/docker-entrypoint-initdb.d
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # MONGODB - Local Development (mirrors Atlas)
  # ===========================================
  mongodb:
    image: mongo:7
    container_name: bizflow-mongodb
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER:-bizflow}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD:-bizflow_dev}
      - MONGO_INITDB_DATABASE=bizflow
    volumes:
      - mongodb-data:/data/db
      - ./05-DATABASES/mongodb-atlas/schemas:/docker-entrypoint-initdb.d
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # N8N - Workflow Automation
  # ===========================================
  n8n:
    image: n8nio/n8n:latest
    container_name: bizflow-n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD:-bizflow_n8n}
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
    volumes:
      - n8n-data:/home/node/.n8n
      - ./06-WORKFLOWS/n8n:/home/node/workflows
    networks:
      - bizflow-network
    restart: unless-stopped

  # ===========================================
  # AGENT ORCHESTRATOR
  # ===========================================
  agent-orchestrator:
    build:
      context: ./02-AGENTS/orchestration
      dockerfile: Dockerfile
    container_name: bizflow-orchestrator
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=${SUPABASE_DATABASE_URL}
      - HUBSPOT_ACCESS_TOKEN=${HUBSPOT_ACCESS_TOKEN}
    depends_on:
      - redis
      - bizflow-backend
    volumes:
      - ./02-AGENTS:/app/agents
    networks:
      - bizflow-network
    restart: unless-stopped

networks:
  bizflow-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  mongodb-data:
  n8n-data:
