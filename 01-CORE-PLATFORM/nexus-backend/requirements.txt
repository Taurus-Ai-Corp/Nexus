# Core framework
# ⚠️  Python 3.10+ required for python-multipart 0.0.22 CVE fix
# Currently running Python 3.9 - upgrade Python to fix CVE GHSA-wp53-j4wj-2cfg
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.11.0
python-multipart==0.0.20  # ⚠️  CVE - Upgrade Python to 3.10+

# HTTP / utilities
requests==2.32.5
python-dotenv==1.0.0
aiofiles==24.1.0
aiohttp==3.13.4          # Multiple CVEs fixed
asyncio

# AI SDKs (updated for security + API compatibility)
anthropic==0.50.0
openai==1.66.0

# Security add-ons
slowapi==0.1.9
structlog==24.4.0

# Monitoring & Error Tracking
sentry-sdk[fastapi]==2.20.0

# Database (use one of the following)
# psycopg2-binary==2.9.10  # PostgreSQL (uncomment for production)
# redis==5.2.1             # Redis for rate limiting (uncomment for production)

# Additional utilities
bcrypt==4.2.1
pyjwt[crypto]==2.12.0      # Security CVEs fixed
